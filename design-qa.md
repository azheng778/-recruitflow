# 侧边栏会话操作设计核验

## 对比对象

- 参考图：`C:\Users\阿正\AppData\Local\Temp\codex-clipboard-a80b62cb-a1a3-4c4b-a133-8e2767e39dfd.png`（555 × 860）
- 实现截图：`D:\AA_hragent\qa-sidebar-conversations.png`（1265 × 712，浏览器默认桌面视窗）
- 同图对比：`D:\AA_hragent\qa-sidebar-comparison.png`（616 × 902）。对比图仅裁取两侧边栏区域；参考图为较窄的应用截图，未按全页尺寸进行等比比较。
- 页面与状态：`http://127.0.0.1:8000/agent`，AI 招聘助手展开、存在历史会话。

## 已验证交互

- 每一个会话行均显示相邻的“重命名”和“删除”按钮。
- 重命名按钮可进入输入状态；按 Escape 会取消编辑并恢复原标题，未写入业务数据。
- 删除按钮连接到已有的会话归档接口；归档会话会从侧边栏列表隐藏，当前会话删除后会回到新建会话页。

## 视觉核验

### 字体与排版

- 保持原有侧边栏标题、会话标题、日期和导航字体尺寸；新增操作按钮使用既有紧凑辅助字号。
- 会话标题独占第一行，日期与两个操作按钮位于第二行。长标题可获得约 190px 的可用行宽，避免与日期和按钮争抢空间。

### 间距与布局

- 未改变应用侧边栏宽度（276px）或主内容布局。
- 会话容器左侧缩进由 32px 调整为 18px，仅用于增加会话选择模块的有效横向空间。
- “重命名 / 删除”采用相邻的紧凑操作组，标题、日期和操作控件无覆盖、无横向溢出。

### 颜色与交互状态

- 复用深绿色侧边栏、浅色文字与既有悬浮背景；删除按钮仅在悬浮/聚焦时显示浅红提示，避免常态干扰。
- 按钮有可见名称与 `aria-label`，键盘可聚焦。

### 图像与文案

- 本次为既有界面局部改动，无新增图片或图标资源。
- 新增文案为“重命名”“删除”“确认删除会话…”，均与现有中文界面一致。

## 结果

未发现 P0、P1 或 P2 级视觉问题。参考图与实现截图的完整视窗尺寸不同，但会话侧边栏区域已经在同一对比图中复核；本次约束的宽度、字体、会话按钮位置和可读性均满足要求。

`final result: passed`
