如果我是面试官，我会建议你不要一开始设计过于复杂的HR系统，而是围绕“候选人从进入系统到完成招聘决策”的完整闭环设计数据库。

## 一、MVP版本建议包含的核心表

### 1. `candidates` 候选人表

保存候选人的基础信息和当前招聘状态。

核心字段：

```
id
name
phone
email
gender
city
source
resume_file_url
resume_text
current_status
created_at
updated_at
```

`current_status`可以包括：

```
new              新入库
screening        筛选中
interview_1      一面
interview_2      二面
offer            已发Offer
hired            已入职
rejected         已淘汰
withdrawn        候选人放弃
```

### 2. `jobs` 职位表

保存招聘岗位信息。

```
id
job_name
department
description
requirements
location
salary_range
status
owner_id
created_at
```

### 3. `candidate_jobs` 候选人-岗位关联表

一个候选人可能应聘多个岗位，一个岗位也会对应多个候选人，因此不建议直接把 `job_id` 放在候选人表中。

```
id
candidate_id
job_id
source
status
match_score
applied_at
updated_at
```

这张表还能保存AI对候选人与岗位的匹配评分。

### 4. `resumes` 简历表

如果你需要体现“简历上传与AI解析”，建议单独拆表，而不是把所有字段都放在候选人表中。

```
id
candidate_id
file_name
file_url
file_type
raw_text
parsed_data
parse_status
parser_model
created_at
```

`parsed_data`可以保存结构化JSON，例如：

```
{
  "education": [],
  "work_experience": [],
  "skills": [],
  "projects": [],
  "years_of_experience": 5
}
```

### 5. `interviews` 面试表

保存每一轮面试的安排和结果。

```
id
candidate_id
job_id
round
interview_type
scheduled_at
duration
interviewers
location
status
strengths
weaknesses
feedback
recommendation
created_by
created_at
updated_at
```

`round`可以是：

```
screening
first
second
final
```

`recommendation`可以是：

```
pending
pass
reject
hold
```

### 6. `candidate_status_history` 候选人状态历史表

这张表非常重要，能够体现招聘流程的可追踪性。

```
id
candidate_id
from_status
to_status
reason
operator_type
operator_id
created_at
```

例如：

```
AI建议：通过一面
HR确认：通过一面
系统将候选人状态更新为：二面
```

这样可以回答面试官经常问的一个问题：

> 如果AI判断错了，能不能追溯是谁、什么时候、为什么修改了状态？

### 7. `communications` 沟通记录表

保存HR与候选人的沟通摘要，不一定要保存所有原始聊天内容。

```
id
candidate_id
channel
direction
content
summary
intent
next_action
operator_id
created_at
```

`channel`可以包括：

```
web
wecom
email
phone
```

你可以让Agent根据沟通内容自动生成：

- 沟通摘要
- 候选人意向
- 薪资期望
- 可入职时间
- 下一步动作

### 8. `notifications` 通知任务表

实现面试前24小时提醒和通知状态追踪。

```
id
candidate_id
interview_id
channel
recipient
content
scheduled_at
sent_at
status
retry_count
error_message
```

`status`：

```
pending
sent
failed
cancelled
```

这张表比直接在代码里 `sleep(24小时)` 更专业，也方便后续接入企业微信、邮件或短信。

### 9. `users` 用户表

保存HR、面试官和管理员。

```
id
name
email
role
department
is_active
created_at
```

角色可以先简单设计为：

```
admin
hr
interviewer
viewer
```

### 10. `audit_logs` 操作审计表

记录所有重要操作，尤其是AI和人工对数据库的修改。

```
id
user_id
action
target_type
target_id
before_data
after_data
source
created_at
```

`source`：

```
human
agent
system
```

这是Human-in-the-loop能够落地的重要基础。

## 二、Agent相关的表

如果你要重点展示 LangGraph、Agent 和多轮对话能力，建议额外增加以下表。

### 11. `agent_conversations` Agent会话表

```
id
user_id
session_id
title
created_at
updated_at
```

### 12. `agent_messages` Agent消息表

```
id
conversation_id
role
content
tool_name
tool_input
tool_output
created_at
```

`role`：

```
user
assistant
tool
system
```

这样你可以在页面上展示：

```
用户问题
→ Agent思考结果
→ 调用工具
→ SQL查询
→ 返回结果
```

不过生产环境不建议把完整内部思考过程直接展示给用户，可以展示“执行了候选人查询工具”，而不是详细思维链。

### 13. `approval_requests` 人工确认表

```
id
candidate_id
interview_id
request_type
proposed_action
proposed_data
status
requested_by
approved_by
approved_at
created_at
```

例如：

```
request_type: interview_result
proposed_action: update_candidate_status
proposed_data: {"status": "rejected"}
status: pending
```

Agent不能直接淘汰候选人，而是创建一条待审批记录，HR确认后才真正执行修改。

## 三、我建议你Demo中的表结构

48小时内不建议全部实现，可以优先使用这9张表：

```
users
jobs
candidates
resumes
candidate_jobs
interviews
candidate_status_history
notifications
audit_logs
```

如果要突出Agent能力，再增加：

```
agent_conversations
agent_messages
approval_requests
```

## 四、核心关系

```
users
  ├── jobs
  ├── interviews
  ├── agent_conversations
  └── audit_logs

candidates
  ├── resumes
  ├── candidate_jobs ─── jobs
  ├── interviews
  ├── candidate_status_history
  ├── communications
  └── notifications

agent_conversations
  └── agent_messages

approval_requests
  └── candidates / interviews
```

## 面试时最重要的表达

你可以这样向面试官解释：

> 我的数据库设计以候选人为核心，以岗位、面试和状态流转为主线。候选人的当前状态用于快速查询，状态历史用于过程追踪；AI产生的面试结论不会直接修改核心数据，而是通过审批表进入Human-in-the-loop流程，HR确认后再更新候选人状态。同时，Agent会话、工具调用、通知任务和审计日志都会被记录，保证招聘流程可追溯、可恢复、可扩展。

这比单纯说“我有一张候选人表和一张面试表”更能体现你对企业系统设计和AI Agent安全性的理解。